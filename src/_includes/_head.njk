<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ renderData.title or title or metadata.title }}</title>
    <meta name="Description" content="{{ renderData.description or description or metadata.description }}">
    <meta property="og:title" content={ title or renderData.title or metadata.title }/>
    <meta property="og:url" content={ page.url } />
    <meta property="og:description" content={description or renderData.description} />
    <meta property="og:image" content="../assets/img/social-card.svg" />
    <meta name="twitter:card" content="summary_large_image" />
    <link rel="preload" href="{{ 'assets/fonts/domaine-text-web-light-subset.woff2' | url }}" as="font" type="font/woff2" crossorigin="crossorigin">
    <link rel="preload" href="{{ 'assets/fonts/calibre-web-regular-subset.woff2' | url }}" as="font" type="font/woff2" crossorigin="crossorigin">
    <link rel="preload" href="{{ 'assets/fonts/calibre-web-bold-subset.woff2' | url }}" as="font" type="font/woff2" crossorigin="crossorigin">

    {% if canonical %}
      <link rel="canonical" href="{{ canonical }}" />
    {% endif %}

    {% set css %}
      {% include "main.min.css" %}
    {% endset %}
    <style type="text/css">
    {{ css | cssmin | safe }}
    </style>

    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
  </head>